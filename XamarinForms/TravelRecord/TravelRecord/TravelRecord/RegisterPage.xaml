<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TravelRecord.RegisterPage">
    <ContentPage.Content>
        <StackLayout x:Name="stackContainer" Margin="20,0,20,0" VerticalOptions="Center">
            <Entry x:Name="emailEntry" 
                   Text="{Binding Email, Mode=TwoWay}" 
                   Placeholder="Email address" 
                   Keyboard="Email"></Entry>
            <Entry x:Name="passEntry" 
                   Text="{Binding Password, Mode=TwoWay}" 
                   Placeholder="password" 
                   IsPassword="True"></Entry>
            <Entry x:Name="confirmPassEntry" 
                   Text="{Binding ConfirmPassword, Mode=TwoWay}" 
                   Placeholder="password"
                   IsPassword="True"></Entry>
            <Button Margin="0,40,0,0" CornerRadius="25" 
                Text="Register" 
                x:Name="registerButton"
                Command="{Binding ConfirmRegisterCommand}"
                CommandParameter="{Binding User}"
                ></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>